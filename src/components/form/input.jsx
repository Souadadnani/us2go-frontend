import { useFormContext } from 'react-hook-form'

export const Input =({})=> {
    return(
        <div>input</div>
    )
}

export const errorInput = () =>{
    return(
        <div>error</div>
    )
}